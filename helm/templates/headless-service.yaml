apiVersion: v1
kind: Service
metadata:
  name: mongodb-headless
  namespace: {{ .Release.Namespace }}
spec:
  clusterIP: None
  selector:
    app: mongodb
  ports:
    - name: mongodb
      port: {{ .Values.service.port }}
      targetPort: 27017